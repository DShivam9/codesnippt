const si = require('react-icons/si');
const keys = Object.keys(si);
console.log('CSS:', keys.filter(k => k.toLowerCase().includes('css')));
console.log('C#:', keys.filter(k => k.toLowerCase().includes('csharp')));
console.log('Java:', keys.filter(k => k.toLowerCase().includes('java')));
